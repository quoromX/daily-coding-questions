**Daily Coding Challenge - Day 5: Minimum Changes to Anagram**

Given two strings `s` and `t` of the same length, return the minimum number of characters you must change in `t` to make it an anagram of `s`.

Assuming that `s` and `t` are always in lowercase.

**Examples**
`minChangesToAnagram("bab", "aba")` returns `1`
*Explanation:*
Change one `"a"` in `"aba"` to `"b"`, making `"abb"`, which is an anagram of `"bab"`.

`minChangesToAnagram("leetcode", "practice")` returns `5`

`minChangesToAnagram("anagram", "mangaar")` returns `0`
*Explanation:*
The strings already contain the same letters.

**Test Cases**
`minChangesToAnagram("a", "b")` = 1
`minChangesToAnagram("xxyyzz", "xxyyzz")` = 0
`minChangesToAnagram("friend", "family")` = 4
`minChangesToAnagram("listen", "silent")` = 0
`minChangesToAnagram("abc", "def")` = 3

```go
func minChangesToAnagram(s, t string) int {
}
```


**Note**
Try to solve without AI. It helps
